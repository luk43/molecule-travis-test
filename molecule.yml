---
driver:
  name: docker
docker:
  containers:
    - name: molecule_user
      image: debian
      image_version: latest
      ansible_groups:
        - group1
      privileged: True
verifier:
  name:
#    n: 1
