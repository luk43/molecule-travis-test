---
driver:
  name: docker
docker:
  containers:
    - name: user
      image: debian
      image_version: latest
      ansible_groups:
        - group1
verifier:
#  name: testinfra  options:
#    n: 1
